import * as React from 'react';
import { View, Text, StyleSheet,TouchableOpacity } from 'react-native';

const ActionPlanHome = ({navigation}) => {
  return (
                  <Text  >
                      Action Plan Screen
                  </Text>
  );
}

export default ActionPlanHome;